/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['image.pollinations.ai'], // Add your image domains here
    },
  };
  
  export default nextConfig;
  